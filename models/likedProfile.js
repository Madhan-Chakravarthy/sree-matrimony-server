const mongoose = require('mongoose');

const LikedProfileSchema = mongoose.Schema({
  
})
// here we are exporting the userData itself
exports.LikedProfile = mongoose.model('liked_profile',LikedProfileSchema);


